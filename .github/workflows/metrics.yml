# Visit https://github.com/lowlighter/metrics/blob/master/action.yml for full reference
name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # ── Authentication ──────────────────────────────────────────────
          # METRICS_TOKEN must have these scopes:
          #   read:user  → required by achievements (sponsorshipsAsSponsor field)
          #   repo       → required by languages indepth (clones private repos)
          # Classic token: check both boxes in GitHub → Settings → Developer settings
          token: ${{ secrets.METRICS_TOKEN }}

          # ── Identity & output ───────────────────────────────────────────
          user: DazedNConfused-
          template: classic
          filename: github-metrics.svg
          output_action: commit
          config_timezone: America/Buenos_Aires
          config_animations: yes

          # ── Base sections ───────────────────────────────────────────────
          base: header, activity, community, repositories, metadata
          base_hireable: no
          repositories: 100
          repositories_forks: yes
          repositories_affiliations: owner, collaborator, organization_member

          # ── Isometric calendar ──────────────────────────────────────────
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

          # ── Languages ───────────────────────────────────────────────────
          commits_authoring: DazedNConfused-, GLopez@Signiant.com, gonzalo.lopez@wbdcontractor.com, gonzalo.lopez@wbconsultant.com, gonzalo.lopez@super7ui.com, gvigliante@mobeats.io, glopezvigliante@gmail.com
          plugin_languages: yes
          plugin_languages_limit: 8
          plugin_languages_threshold: 0%
          plugin_languages_details: bytes-size, percentage
          plugin_languages_indepth: yes
          plugin_languages_ignored: C++, C#, HTML, Visual Basic, PLSQL
          plugin_languages_skipped: CoverAO

          # ── Coding habits ───────────────────────────────────────────────
          # plugin_habits_charts disabled: with sparse commit activity the plugin
          # crashes computing Math.max() on an empty hour/day set (no events in window).
          # Facts-only mode is safe regardless of activity level.
          plugin_habits: yes
          plugin_habits_from: 200
          plugin_habits_days: 30
          plugin_habits_facts: yes
          plugin_habits_charts: no

          # ── Lines of code ───────────────────────────────────────────────
          plugin_lines: yes
          plugin_lines_sections: base, repositories
          plugin_lines_repositories_limit: 4

          # ── Issue & PR follow-up ────────────────────────────────────────
          plugin_followup: yes
          plugin_followup_sections: repositories, user
          plugin_followup_indepth: yes

          # ── Reactions ───────────────────────────────────────────────────
          plugin_reactions: yes
          plugin_reactions_limit: 200
          plugin_reactions_limit_issues: 100
          plugin_reactions_days: 365
          plugin_reactions_display: relative
          plugin_reactions_details: percentage

          # ── People ──────────────────────────────────────────────────────
          plugin_people: yes
          plugin_people_types: followers, following
          plugin_people_limit: 28
          plugin_people_size: 28
          plugin_people_shuffle: yes

          # ── Achievements ────────────────────────────────────────────────
          # Requires read:user scope — the graphql query accesses sponsorshipsAsSponsor
          # which is gated behind that scope. Without it the query throws and the
          # whole plugin fails with "Unexpected error".
          plugin_achievements: yes
          plugin_achievements_threshold: C
          plugin_achievements_secrets: yes
          plugin_achievements_display: compact
          plugin_achievements_limit: 0

          # ── Notable contributions ────────────────────────────────────────
          plugin_notable: yes
          plugin_notable_from: all
          plugin_notable_self: yes
          plugin_notable_types: commit, pull_request, issue
          plugin_notable_indepth: yes
          plugin_notable_repositories: yes

          # ── Topics ──────────────────────────────────────────────────────
          # icons mode: shows the topic's logo instead of a text label.
          # Topics without an official icon are silently omitted.
          plugin_topics: yes
          plugin_topics_limit: 20
          plugin_topics_mode: icons
          plugin_topics_sort: stars

          # ── Recently starred ─────────────────────────────────────────────
          plugin_stars: yes
          plugin_stars_limit: 4
